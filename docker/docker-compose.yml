version: '3'
services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - DEBUG=false
      - DB_USER="postgres"
      - DB_PASS="postgres"
      - DB_NAME="bouffebot_dev"
     
